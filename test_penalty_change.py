def test_penalty_change(driver, old_penalty_count, new_penalty_count):
    print(
        "I am a hook firing b/c a driver faces a penalty change",
        driver,
        old_penalty_count,
        new_penalty_count,
    )
